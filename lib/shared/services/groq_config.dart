/// Groq API key for quiz generation. Get one free at
/// https://console.groq.com/keys — no credit card required, ever, for the
/// free tier (14,400 requests/day, resets daily).
///
/// IMPORTANT: don't commit a real key to a public repo. Add this file to
/// .gitignore if your repo is public, or swap this for a --dart-define /
/// environment variable setup later.
const String groqApiKey = 'API-KEY';